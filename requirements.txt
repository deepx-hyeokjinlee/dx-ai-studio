# DX AI Studio runtime dependencies are declared in pyproject.toml and installed
# by `pip install -e .` (currently: beautifulsoup4 + requests, used only by the
# dx_app ModelZoo tab). Everything else runs on the Python standard library alone
# (Python 3.12+). Test/CI extras live in requirements-ci.txt.
